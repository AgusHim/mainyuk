{
    "version": 2,
    "builds": [
      {
        "src": "package.json",
        "use": "@vercel/node"
      }
    ],
    "build": {
      "env": {
        "FORCE_INSTALL": "true"
      }
    },
    "scripts": {
      "build": "npm install --force && npm run build"
    }
  }
